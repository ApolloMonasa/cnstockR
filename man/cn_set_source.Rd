% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{cn_set_source}
\alias{cn_set_source}
\title{设置默认数据源}
\usage{
cn_set_source(
  source = c("auto", "eastmoney", "sina", "netease"),
  fallback_sources = NULL
)
}
\arguments{
\item{source}{默认源，可选 `"auto"`、`"eastmoney"`、`"tencent"`、`"sina"`、`"netease"`。}

\item{fallback_sources}{可选字符向量。仅在 `source = "auto"` 时生效，
表示自动模式下的候选源顺序。}
}
\value{
不可见地返回更新后的源配置列表。
}
\description{
配置全局默认数据源。若设为 `"auto"`，将按 `fallback_sources` 顺序自动回退。
}
\examples{
# 自动模式并指定回退顺序
cn_set_source("auto", fallback_sources = c("eastmoney", "tencent", "sina", "netease"))

# 固定单一源
cn_set_source("sina")
}
